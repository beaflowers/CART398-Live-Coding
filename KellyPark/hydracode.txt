//kelly's day in the park 
s1.initVideo("https://raw.githubusercontent.com/beaflowers/CART398-Live-Coding/main/KellyPark/Walking.mp4")
s3.initVideo("https://raw.githubusercontent.com/beaflowers/CART398-Live-Coding/main/KellyPark/walking2.mp4")

a.show()

a.setBins(4)     // number of frequency bins (default: 4)
a.setSmooth(0.4) // smoothing 0–1 (default: 0.4)
a.setCutoff(4)   // noise gate — values below this ? 0
a.setScale(1)    // output amplitude scaling

a.fft[0]  // bin 0 — typically sub-bass
a.fft[1]  // bin 1 — bass / low-mids
a.fft[2]  // bin 2 — mids
a.fft[3]  // bin 3 — highs
 
//paranoid texture
voronoi(350,0.55)
  	.modulateScale(osc(4).rotate(Math.sin(time/2)),.8)
  	.thresh(.8)
	.modulateRotate(osc(3),.2)
  	//.diff(src(o0).scale(5.8))
	.modulateScale(osc(2).modulateRotate(o0,.4, 4))
	.diff(src(o0).rotate([3,1,2,4]).scrollY(0,[-1,0])) //dizzy spinning 
	.brightness([-.2,-.57].smooth().fast(.5))
	.color(() => Math.sin(time) + 1,1,() => Math.cos(time) + 1)
 	.saturate(4)
	.add(osc(10, 0.1, 1.2))
    .diff(shape(16, 0.6).rotate(0, 0.1).scale(()=>Math.sin(time*4)+1))
	.out(o0)

//walking video
src(s1)
	//.mask(src(o0))
    .out(o1)

render(o1)

//pleasure texture
osc(7, 0.2, 9)
	.posterize(30)
	.rotate(()=>Math.sin(time)-2)
	.scale(()=>Math.cos(time)+2)
	.saturate(3)
	.brightness(-1.5)
	.add(noise(2, 0.4, 9).colorama(0.5).posterize(3))
	.modulate(o3, 1) //bump up amount for crunch
	.out(o2)

//kelly video
src(s3)
	.diff(o2, 9)
	.out(o3)

//render(o3)

//hush()


